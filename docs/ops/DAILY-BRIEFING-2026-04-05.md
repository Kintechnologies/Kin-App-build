# Kin AI — Daily Briefing

📅 Day 5 of 14 — Sunday, April 5, 2026

**SPRINT STATUS**
Stages 1–3 ✅ complete. Stage 4 personal thread done; household thread blocked on IE (`household-chat-prompt.md` — 11+ cycles overdue, escalated). Stage 5 blocked on Austin B2. Sprint ~8 days ahead of schedule.

**TODAY'S PRIORITY**
Finish RevenueCat (B2): iOS app + products still need RC dashboard config (`kin_monthly_3999` + `kin_annual_29900`). API key is already in `.env` — just needs the dashboard side done. This is the only P0 blocking TestFlight.

**BLOCKERS**
- 🔴 B2 — RevenueCat iOS app + products not in RC dashboard. Env key set ✅; dashboard config not. Blocks S5.2.
- 🔴 IE — `household-chat-prompt.md` 11+ cycles overdue. Household thread §16 compliance not enforced. S4.6 e2e blocked.
- 🟡 B4 — Google OAuth verification still not submitted. 4–6 week clock hasn't started. Every day is a day closer to launch risk.

**AUSTIN'S ACTIONS NEEDED**
1. **RevenueCat dashboard** — add iOS app (bundle ID + App Store Connect), create `kin_monthly_3999` + `kin_annual_29900` products
2. **Google OAuth** — submit verification now (logo, homepage/privacy/ToS URLs, authorized domain). Don't let another day slip.
3. **`supabase db push`** — unblocked since last night; applies migrations 027 + 028 (alert severity writes failing silently in prod until this runs)
4. **`git push`** from terminal to sync agent work
5. **Delete** stale `docs/prompts/docs/` from terminal: `rm -rf docs/prompts/docs`
6. **Decide** check-in wiring: cron/event-triggered vs. inline at Today-screen load (Lead Eng blocked)

**AGENTS WORKING ON**
Lead Eng: P2-23 — extending household context block in `route.ts` to fetch partner calendar events before IE prompt is wired. QA: standing by to audit household e2e once IE delivers. IE: must deliver `household-chat-prompt.md` immediately.

---
_Generated by CoS Coordinator — 2026-04-05 07:00_
