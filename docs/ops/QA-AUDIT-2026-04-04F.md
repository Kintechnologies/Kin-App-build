# QA Audit — 2026-04-04F
**Run:** F (odd-hour :00 — auditing Lead Eng run AC)
**Auditor:** QA & Standards Lead
**Session context:** Auditing Lead Eng run AC, which resolved P2-16 — general thread `renderItem` preview fallback removed from `chat.tsx` ~line 617.

---

## Files Audited

| File | Reason |
|------|--------|
| `apps/mobile/app/(tabs)/chat.tsx` | P2-16 verification; null contract consistency across all thread types |
| `apps/mobile/app/(tabs)/_layout.tsx` | Architecture check — 3-tab conformance |
| `supabase/migrations/` (024–027) | Migration presence re-confirmation |
| `docs/prompts/morning-briefing-prompt.md` | P2-7 — INPUT FORMAT status check |
| `docs/prompts/` | household-chat-prompt.md delivery check |

---

## ✅ Verified RESOLVED This Run

### P2-16 — General thread preview shows "No messages yet" placeholder ✅ RESOLVED

**File:** `apps/mobile/app/(tabs)/chat.tsx` lines 616–620
**Code (post-fix):**
```jsx
{thread.preview ? (
  <Text style={styles.threadPreview} numberOfLines={1}>
    {thread.preview}
  </Text>
) : null}
```
`"No messages yet"` fallback string is gone. Empty preview renders `null`, consistent with the P2-12 null contract applied to pinned threads in run AA.

Null contract is now uniform across all three thread types:
- Personal thread (~line 534): `{personalThread.preview ? <Text...>{personalThread.preview}</Text> : null}` ✅
- Household thread (~line 583): `{householdThread.preview ? <Text...>{...}</Text> : null}` ✅
- General thread (~line 616): `{thread.preview ? <Text...>{thread.preview}</Text> : null}` ✅

**P2-16 is closed.** ✅

---

## Architecture Audit

| Check | Result |
|-------|--------|
| `_layout.tsx` — exactly 3 tabs (index, chat, settings) | ✅ PASS |
| No domain tabs (meals/budget/fitness/family) in tab bar | ✅ PASS — 3 entries only |
| Domain files still exist (not deleted) | ✅ PASS — budget.tsx, family.tsx, fitness.tsx, meals.tsx all present in `app/(tabs)/` |
| `024_coordination_issues.sql` migration exists | ✅ PASS |
| `025_chat_thread_types.sql` migration exists | ✅ PASS |
| `026_kin_check_ins.sql` migration exists | ✅ PASS |
| `027_coordination_issues_severity.sql` migration exists | ✅ PASS (awaiting Austin `supabase db push` — B29 still open) |

---

## Code Quality — Changed File

Run AC changed exactly one file (`chat.tsx`) with a single conditional expression change (line 617 area). No new code paths introduced.

| Check | File | Result |
|-------|------|--------|
| No bare `console.error` | `chat.tsx` | ✅ Single instance at line 360, properly gated with `process.env.NODE_ENV !== "production"` — unchanged from run E |
| No `any` TypeScript types | `chat.tsx` | ✅ Clean — no `any` types present |
| No unused imports | `chat.tsx` | ✅ No new imports added this session |
| All async operations have try/catch | `chat.tsx` | ✅ No new async operations added this session |
| tsc --noEmit (Lead Eng self-report) | mobile | ✅ 0 new errors (pre-existing `push-notifications.ts` errors unchanged, unrelated) |

---

## Standing Open Issues (status confirmation)

### P1 — household-chat-prompt.md STILL MISSING (IE action)
**Path:** `docs/prompts/household-chat-prompt.md`
**Status:** File does not exist. Confirmed missing again this run.
**Impact:** Household thread (`chat.tsx` → `/api/chat/route.ts`) continues to use `chat-prompt.md` (personal thread prompt). No §16 routing logic for household coordination conflicts, ambiguous responsibility, or resolution confirmations. Household thread outputs may assign responsibility or use personal tone when collective/collaborative tone is required per spec.
**Spec:** §16 — balanced, neither parent called out; collaborative framing for shared conflicts.
**Owner:** Intelligence Engineer (S1.8)
**Severity:** P1 — §16 compliance gap
**Open since:** QA Run R. No action this cycle. CoS to continue monitoring.

---

### P2-7 — INPUT FORMAT mismatch in morning-briefing-prompt.md (IE action)
**File:** `docs/prompts/morning-briefing-prompt.md` (lines 50–56)
**Status:** INPUT FORMAT still specifies structured JSON input (`today_events`, `pickup_assignments`, `household_conflicts`, `last_coordination_change`, `current_time`). Route.ts sends unstructured text (`briefingContext` string — confirmed by grep of route.ts lines 139–308).
**Impact:** Future IE edits to INPUT FORMAT in the prompt file will have no effect on the route. Spec-to-implementation drift is currently silent.
**Owner:** Intelligence Engineer (S1.8)
**Severity:** P2
**Open since:** QA Run Q. No change this run.

---

### B31/P2-5 — Stale `docs/prompts/docs/` directory (Austin action)
**Status:** `docs/prompts/docs/` directory still exists. Confirmed present this run (`ls docs/prompts/` shows `docs` subdirectory).
**Impact:** No functional impact — correct files at `docs/prompts/` are wired. Risk: future IE edits going to the wrong path.
**Owner:** Austin (read-only mount; AI cannot delete)
**Command:** `rm -rf docs/prompts/docs`
**Open since:** QA Run Q. No change.

---

### Architecture note — chat history not filtered by thread_id
**File:** `apps/web/src/app/api/chat/route.ts` ~lines 270–275
**Status:** Unchanged. Query uses `.eq("profile_id", user.id)` without a `thread_id` filter. All conversation history (personal + household) loads into the same context window.
**QA position:** Low TestFlight risk (household thread usage will be minimal for v0 beta). Post-launch P2 debt. No action required before TestFlight.

---

## What Passed Clean

- P2-16: General thread empty preview → null ✅ (consistent null contract across all 3 thread types now enforced)
- Architecture: 3-tab shell clean, domain files intact ✅
- Migrations 024–027 all present ✅
- No bare `console.error`, no TypeScript `any`, no unused imports in `chat.tsx` ✅
- tsc 0 new errors ✅
- Pinned thread null contract (P2-12, run AA) still intact ✅

---

## Summary

**P0 issues:** None

**P1 issues:**
- `household-chat-prompt.md` missing → household thread uses personal chat prompt (§16 compliance gap). IE action (S1.8). Open since run R.

**P2 issues (new):** None

**P2 issues (standing):**
- P2-7: INPUT FORMAT mismatch in `morning-briefing-prompt.md` (IE action, S1.8)
- B31/P2-5: Stale `docs/prompts/docs/` directory (Austin action)
- Chat history not filtered by thread_id (post-launch architectural debt, no TestFlight blocker)

**Austin-blocked:**
- B29: `supabase db push` for migration 027
- B31: delete stale `docs/prompts/docs/`

**Lead Eng status:** No open code tasks. All P1/P2 items resolved. Waiting on IE `household-chat-prompt.md` (S4.6) and Austin B2 (Stage 5).

---

_— QA & Standards Lead, 2026-04-04 run F_
