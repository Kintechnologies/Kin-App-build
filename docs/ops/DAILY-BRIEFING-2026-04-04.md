# Kin AI — Daily Briefing

📅 Day 4 of 14 — Saturday, April 4, 2026

**SPRINT STATUS**
Stages 1–3 complete ✅. Stage 4 is one fix from done (B30: chat-prompt migration). Sprint ~8 days ahead of schedule.

**TODAY'S PRIORITY**
Complete RevenueCat setup (B2): add iOS app (bundle ID + ASC), create `kin_monthly_3999` + `kin_annual_29900` products, drop `EXPO_PUBLIC_REVENUECAT_API_KEY` in `.env`. This is the only P0 blocking TestFlight.

**BLOCKERS**
- 🔴 B2 — RevenueCat iOS app + products not configured. Blocks S5.2 (TestFlight). Austin action.
- 🟡 B4 — Google OAuth verification not yet submitted. 4–6 week clock hasn't started. Austin action.
- 🟡 B29 — Migration `027_coordination_issues_severity.sql` not applied in prod. Run `supabase db push` or alert writes will fail silently. Austin action.
- 🟡 B30 — `/api/chat/route.ts` still using `buildSystemPrompt` (Lead Eng, next session).

**AUSTIN'S ACTIONS NEEDED**
1. **RevenueCat** — configure iOS app + products in RC dashboard, add API key to `.env`
2. **Google OAuth** — submit verification (logo, homepage, privacy/ToS URLs, `kinai.family` domain)
3. **`supabase db push`** — apply migration 027 before alert cards go live
4. **Delete** `docs/prompts/docs/` from terminal (stale duplicate prompt dir — P2-5/B31)
5. **Decide** check-in wiring: cron/event-triggered vs. inline at Today-screen load (Lead Eng is blocked)
6. **`git push`** from terminal after Lead Eng completes B30

**AGENTS WORKING ON**
Lead Eng: B30 (migrate `/api/chat/route.ts` → `chat-prompt.md`), then P2-10 fallback text fix in `index.tsx`, then S4.6 full e2e audit. QA: standing by to audit B30 fix and S4.6.

---
_Generated by CoS Coordinator — 2026-04-04 07:00_
